% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mendFull.R
\name{mendFull}
\alias{mendFull}
\title{Shortuct 'mendRoot()' argument path (via 'run = full')to evaluate a parameter set and time series for fully analysed and calibrated output.  Functionally the same as arugment path via 'calibrate = TRUE' without the option of further optimization.}
\usage{
mendFull(fileInput = fileInput, fileTimeSeries = fileTimeSeries,
  times = times, conditions = conditions, CUE = CUE, model = model,
  maxOpt = maxOpt)
}
\arguments{
\item{fileInput}{// input source for 'run' arugment 'load' or 'object', can be NULL}

\item{fileTimeSeries}{// time-series input source required for calibration}

\item{times}{// initial time sequence for evaluation by 'mendMod()'}

\item{conditions}{// intial temperature and pH values}

\item{CUE}{// User specified boolean for dynamic Carbon Use Efficiency (CUE) for use in evaluation by 'mendMod()'}

\item{model}{// User specified argument to evaluate model along "Steady", Average", "Maximum", or "Minimum" values, or "Ranges", or "All"}

\item{maxOpt}{// User specified boolean to optimize the range of the model according to given function arguement 'model'}
}
\value{
solveData list object containing standard deSolve object via 'deSolve::ode()' and parameter list specific to model output via 'mendMod()'
}
\description{
Shortuct 'mendRoot()' argument path (via 'run = full')to evaluate a parameter set and time series for fully analysed and calibrated output.  Functionally the same as arugment path via 'calibrate = TRUE' without the option of further optimization.
}
\examples{
varName <- mendFull(fileIn, fileTime, times, conditions, CUE, model, maxOpt)

}
